#include "uhead.h"
int main()
{
	showMenu();
	/*
	char filename[100]="1.c";
	char buf[1024]="helloword";
	CreateFile(filename);
	WriteFile(filename,buf);
	ReadFile(filename);
	ChmodFile(filename,S_IRWXG);
	StatFile(filename);*/
	return 0;
}
